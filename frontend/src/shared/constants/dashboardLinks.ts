/**
 * URLs externas del dashboard / sidebar.
 * `null` = enlace aún no definido: la UI no debe ofrecer un href creíble.
 */
export const SELF_PORTAL_URL: string | null = null;
/** Ruta interna del portal (productor). */
export const BIBLIOTECA_INTRANET_PATH = '/intranet/biblioteca';
/** Ruta interna de gestión (admin / superadmin). */
export const BIBLIOTECA_ADMIN_PATH = '/admin/biblioteca';
