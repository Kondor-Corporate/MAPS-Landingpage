# Copiar a .env en esta carpeta (frontend/)
# El navegador del host consume esta URL (no la red interna de Docker).
# En Windows, preferir 127.0.0.1 sobre localhost para evitar cuelgues por IPv6.
VITE_API_BASE_URL=http://127.0.0.1:3000/api/v1
